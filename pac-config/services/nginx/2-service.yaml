apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: bitiq-local
spec:
  selector:
    app: nginx
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
