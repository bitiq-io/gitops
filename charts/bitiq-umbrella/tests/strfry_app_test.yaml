suite: Strfry Application
templates:
  - templates/app-strfry.yaml
values:
  - ../values-common.yaml
release:
  name: umbrella
  namespace: openshift-gitops
tests:
  - it: renders Application when enabled
    set:
      strfry:
        enabled: true
      env: local
    asserts:
      - equal:
          path: kind
          value: Application
      - equal:
          path: metadata.name
          value: strfry-local
      - equal:
          path: spec.source.path
          value: charts/strfry
