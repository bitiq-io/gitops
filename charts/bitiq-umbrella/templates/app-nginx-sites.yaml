{{- $ns := default (dict) .Values.nginxSites }}
{{- $nsEnabled := false }}
{{- if hasKey $ns "enabled" }}
  {{- $nsEnabled = eq (lower (printf "%v" (get $ns "enabled"))) "true" }}
{{- end }}
{{- if $nsEnabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-sites-{{ .Values.env }}
  namespace: {{ .Values.gitopsNamespace }}
  labels:
    bitiq.io/env: {{ .Values.env }}
    app.kubernetes.io/part-of: bitiq-umbrella
spec:
  project: default
  source:
    repoURL: {{ .Values.repoUrl }}
    targetRevision: {{ .Values.targetRevision }}
    path: pac-config/services/nginx
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.appNamespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 2m
    syncOptions:
      - CreateNamespace=true
{{- end }}

