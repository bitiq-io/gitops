{{- $ollama := default (dict) .Values.ollama }}
{{- $ollamaEnabled := false }}
{{- if hasKey $ollama "enabled" }}
  {{- $ollamaEnabled = eq (lower (printf "%v" (get $ollama "enabled"))) "true" }}
{{- end }}
{{- if $ollamaEnabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ollama-{{ .Values.env }}
  namespace: {{ .Values.gitopsNamespace }}
  labels:
    bitiq.io/env: {{ .Values.env }}
    app.kubernetes.io/part-of: bitiq-umbrella
spec:
  project: default
  source:
    repoURL: {{ .Values.repoUrl }}
    targetRevision: {{ .Values.targetRevision }}
    path: charts/ollama
    helm:
      valueFiles:
        - values-common.yaml
        - values-{{ .Values.env }}.yaml
      ignoreMissingValueFiles: true
      parameters:
        - name: baseDomain
          value: {{ .Values.baseDomain | quote }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.appNamespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 2m
    syncOptions:
      - CreateNamespace=true
{{- end }}
