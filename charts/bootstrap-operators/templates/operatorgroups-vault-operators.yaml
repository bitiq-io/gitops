{{- $vso := .Values.operators.vso -}}
{{- if and $vso (ne $vso.installNamespace "openshift-operators") }}
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ printf "%s-og" $vso.installNamespace }}
  namespace: {{ $vso.installNamespace }}
spec: {}
---
{{- end }}
{{- $vco := .Values.operators.vco -}}
{{- if and $vco (ne $vco.installNamespace "openshift-operators") }}
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ printf "%s-og" $vco.installNamespace }}
  namespace: {{ $vco.installNamespace }}
spec: {}
{{- end }}

