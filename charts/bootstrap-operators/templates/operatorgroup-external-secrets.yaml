{{- $eso := .Values.operators.externalSecrets -}}
{{- if and $eso (default true $eso.enabled) (ne $eso.installNamespace "openshift-operators") }}
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ printf "%s-og" $eso.installNamespace }}
  namespace: {{ $eso.installNamespace }}
spec: {}
{{- end }}

