{{- $eso := .Values.operators.externalSecrets -}}
{{- if and $eso (ne $eso.installNamespace "openshift-operators") $eso.createNamespace }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ $eso.installNamespace | quote }}
  labels:
    app.kubernetes.io/managed-by: Helm
{{- end }}
