baseDomain: ""

image:
  repository: nginxinc/nginx-unprivileged
  tag: 1.25-alpine
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  port: 8080
  type: ClusterIP

route:
  # Explicit FQDN to publish the site (recommended)
  host: ""
  # Optional cert-manager ClusterIssuer name for TLS
  clusterIssuerName: ""

