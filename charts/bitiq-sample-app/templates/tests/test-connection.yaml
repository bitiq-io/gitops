apiVersion: v1
kind: Pod
metadata:
  name: "bitiq-sample-app-test"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: registry.access.redhat.com/ubi9/ubi-minimal
      command: ['sh', '-c', 'echo "helm test placeholder"; exit 0']
  restartPolicy: Never