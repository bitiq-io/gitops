suite: Route host formatting
templates:
  - templates/route.yaml
release:
  name: sample
  namespace: bitiq-local
tests:
  - it: builds host from hostPrefix and baseDomain
    set:
      baseDomain: apps.example.test
      backend:
        hostPrefix: svc
    asserts:
      - equal:
          path: spec.host
          value: svc.apps.example.test
